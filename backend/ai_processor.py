# Simulates OpenAI content generation (requires OpenAI API key for real use)
def generate_content(trend_data, api_key=None):
    # Placeholder: Generates mock content based on trends
    return [
        f"New post idea for {trend['hashtag']}: Create a fun video showcasing this trend!"
        for trend in trend_data
    ]